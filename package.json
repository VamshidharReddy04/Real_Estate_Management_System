{
  "name": "real-estate-management-system",
  "private": true,
  "version": "1.0.0",
  "description": "Convenience scripts for running backend and frontend together",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "ports:free": "kill-port 5000 5173",
    "dev": "npm run ports:free && concurrently -n backend,frontend -c yellow,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --port 5173 --strictPort\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend -- --port 5173 --strictPort",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "kill-port": "^2.0.1"
  }
}
